`표현식`<sup>expression</sup>은 값으로 평과될 수 있는 `문`<sup>statement</sup>이다. 즉, 표현식이 평가되면 새로운 값을 생성하거나 기존 값을 참조한다.

리터럴은 값으로 평가되므로 리터럴도 표현식이다.

```
var sum = 10 + 10;
```

10 + 10은 리터럴과 연산자로 이루어져 있다. 이때, 10 + 10은 숫자 값 20을 생성하므로 표현식이다.

표현식은 값으로 평가되므로 문법적으로 값이 위치할 수 있는 자리에는 표현식도 위치할 수 있다.
